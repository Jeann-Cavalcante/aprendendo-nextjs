/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['https://via.placeholder.com', 'via.placeholder.com', 'via.placeholder.com/300x300', 'via.placeholder.com/600x600', 'via.placeholder.com/900x900'],
  },
}

module.exports = nextConfig
